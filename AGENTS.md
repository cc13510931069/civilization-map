# 项目定位

**项目名**：《文明的地图 HD》

**项目路径**：`/Users/ccyu/Documents/文明的地图HD`

**目标用户**：初高中生

**核心目标**：通过地图探索、证据收集和 AI 引导思考，培养学生的文明解释能力，不是普通知识点记忆工具。

# 内容结构

所有文明探索内容必须严格依据《文明的地图》（张信刚）一书结构：

1. 文明研究（方法论）
2. 丝绸之路
3. 大中亚的点与面
4. 印度速写
5. 波斯文明圈
6. 高加索巡礼（当前开发模板）
7. 土耳其进行曲
8. 第37章：古丝绸之路综合探索
9. 第38章：新丝绸之路现实连接

不得自行扩展成普通世界文明课程。

# 核心学习闭环

```
地图探索 → 发现证据 → 阅读补充 → 五步文明思考 → Veggie 辅助 → 形成文明解释 → 我的文明地图
```

# 五步思考框架

所有区域通用：

1. **在哪里？** — 空间位置
2. **有什么条件？** — 自然环境、资源、交通
3. **谁在那里活动？** — 民族、国家、文明
4. **发生了什么变化？** — 交流、冲突、迁徙、融合
5. **我的解释是什么？** — 形成个人文明观点

# Veggie（菜狗）

**中文名**：菜狗
**英文名**：Veggie
**定位**：AI 文明探索导师、文明探索伙伴

不是答案机器人。Veggie 的行为优先：

- 提问
- 引导观察
- 引导寻找证据
- 建立因果关系
- 鼓励形成个人解释

# 架构原则

高加索模块是未来所有文明探索模块的**技术模板**。

未来新增区域应当配置化复用 `RegionModule`：

```
RegionModule
├ 地图
├ 图层
├ 探索节点
├ 证据点
├ 阅读材料
├ 思考任务
├ AI 辅导
├ 评价体系
└ 我的文明地图成长
```

但在进入独立 RegionModule 阶段前，不得自行进行大规模架构重构。

# 技术环境

| 项目 | 值 |
|---|---|
| Flutter | 3.44.6 stable |
| Dart | 3.12.2 |
| 状态管理 | Riverpod（flutter_riverpod 2.6.1） |
| 路由 | GoRouter / ShellRoute |
| 布局 | iPad 横屏优先 |
| 包管理 | Swift Package Manager（不强制 CocoaPods） |
| 测试设备 | iPad Pro 13-inch (M5)，模拟器 ID `FA2DA204-2AE3-4311-B577-A1CC53B8C1C9` |

# 开发纪律

- 修改前先完整阅读实现和现有测试
- 优先最小修复，避免大范围重构
- **不删除失败测试**，**不添加 `skip`**，**不降低断言标准**
- 不使用 `dynamic`、`Object` 或 `cast` 掩盖类型问题
- 不使用 `sed`、Python `str.replace` 或正则批量修改 Dart 结构
- 不用 `try/catch` 吞掉 Widget 布局异常
- 不用 `target`、`expected`、`should pass` 代替实际运行结果
- 必须等待命令完整结束
- 类移动、模型统一或 Widget 结构修改后要冷启动模拟器验证
- GoRouter 项目中**不使用 `Navigator.pushNamed`** 切换主栏目

# 验证要求

每个阶段至少运行：

```bash
dart format .
flutter analyze
flutter test --reporter expanded
```

涉及 iPad UI 时还必须：

- 模拟器冷启动
- 人工点击、滚动和导航验收
- 检查红屏、RenderFlex overflow、ParentDataWidget 错误、状态丢失
- 不能只依赖 Widget 测试

# 插件

插件只有在用户通过 `@` 菜单真实选中并确认可用后才能声称调用。插件不可用时诚实报告，由用户手动进行模拟器验收。
